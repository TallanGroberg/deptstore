<h1> <%= @dept.id ? 'edit Dept' : 'Create Dept' %></h1>
<hr />
<%= form_for [@shop, @dept] do |f| %>
<%= f.label :name %>
<%= f.text_field :name, required: true, autofocus: true %>
<br />
<%= f.label :body %>
<%= f.text_area :body, required: true %>
<%= f.submit %>
<% end %>
